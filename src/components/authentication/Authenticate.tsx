import React from 'react';

const Authenticate: React.FC<any> = (props) => {
    return (
        <React.Fragment>
        <h1>Login</h1>
    </React.Fragment>
    );
    
}


export default Authenticate;